spec_version: 2
description: An AWS EC2 Instance (Virtual Machine), provisioned from a list of AWS built-in AMIs

grains:
  release:
    kind: shell
    spec:
      agent:
        name: demo-dev
      activities:
        deploy:
          commands:
            - "apt-get -y install curl"
            - "echo curl https://raw.githubusercontent.com/TomerAdmon/Torque-Samples/main/assets/scripts/release.sh -o ~/release.sh"
            - "curl https://raw.githubusercontent.com/TomerAdmon/Torque-Samples/main/assets/scripts/release.sh -o ~/release.sh"
            - "echo this is the dir:" 
            - "ls -l /root/release.sh"
            - "sh /root/release.shrelease.sh '{{ sandboxid | downcase }}' 'this is a token'"

